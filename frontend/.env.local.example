# Relative URL — Next.js rewrites proxy /api/* → http://localhost:3000/api/*
# This avoids mixed-content errors when the frontend is served over HTTPS
# (ngrok, production) and prevents the browser from trying to reach the
# backend directly (which fails when accessed from a different machine).
NEXT_PUBLIC_API_URL=/api
